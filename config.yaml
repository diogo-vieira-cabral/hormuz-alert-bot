# ─────────────────────────────────────────────────────────────────────────────
# config.yaml — Hormuz Alert Bot
# ─────────────────────────────────────────────────────────────────────────────
# The only file you edit to change bot behaviour.
# Bot engine lives in main.py — no need to touch it.
#
# Source philosophy:
#   We use wire services only (Reuters, AP). Both are:
#     - Staffed 24/7 with regional correspondents
#     - Editorially disciplined: they don't publish until confirmed
#     - Fast: typically under 5 minutes from event to feed
#   General news aggregators (Google News, BBC, Al Jazeera) were removed
#   because they republish wire copy with a delay, adding latency and
#   duplicate alerts without adding signal.
#
# Keyword philosophy:
#   Keywords must imply CHANGE, not static situation.
#   "hormuz" matches "the strait remains closed" — useless.
#   "reopens" matches only when something actually changes.
# ─────────────────────────────────────────────────────────────────────────────

topic: "HORMUZ"

check_interval_minutes: 5

# Minimum hours before the same article can alert again.
# Prevents duplicate pings when Reuters and AP both cover the same event.
alert_cooldown_hours: 2


# ─────────────────────────────────────────────────────────────────────────────
# TIERS
# Evaluated in order — most severe first. First match wins.
# notify: false = logged silently, no Telegram ping.
# ─────────────────────────────────────────────────────────────────────────────

tiers:

  - label: "CRITICAL"
    emoji: "🚨"
    notify: true
    keywords:
      # Strait status — change words only
      - "reopens"
      - "reopen"
      - "strait opens"
      - "hormuz opens"
      - "passage restored"
      - "transit resumed"
      - "blockade lifted"
      - "strait closed"
      - "hormuz closed"
      # Military events — new incidents only
      - "missile strike"
      - "airstrike"
      - "tanker seized"
      - "vessel seized"
      - "ship seized"
      - "tanker attacked"
      - "mine detected"
      - "mine laid"
      - "naval mine"
      - "ceasefire collapses"
      - "ceasefire violated"
      - "ceasefire ends"
      - "war resumes"
      - "nuclear"

  - label: "HIGH IMPACT"
    emoji: "⚠️"
    notify: true
    keywords:
      # Diplomacy — active developments only
      - "ceasefire agreed"
      - "ceasefire announced"
      - "peace talks"
      - "deal reached"
      - "iran agrees"
      - "iran rejects"
      - "trump ultimatum"
      - "sanctions lifted"
      - "sanctions imposed"
      - "un resolution"
      # Market dislocation — not routine price moves
      - "oil surges"
      - "crude surges"
      - "oil spikes"
      - "energy crisis"
      - "force majeure"
      - "shipping suspended"
      - "carrier suspends"

  - label: "UPDATE"
    emoji: "📡"
    notify: false
    keywords:
      - "hormuz"
      - "strait of hormuz"
      - "persian gulf"


# ─────────────────────────────────────────────────────────────────────────────
# FEEDS
# Wire services only. Both publish 24/7, both are fast and confirmed.
# ─────────────────────────────────────────────────────────────────────────────

feeds:
  # Reuters — world news feed. Faster than topNews for regional events.
  - https://feeds.reuters.com/reuters/worldNews

  # Associated Press — top news. Independent sourcing, complements Reuters.
  - https://feeds.apnews.com/rss/apf-topnews
